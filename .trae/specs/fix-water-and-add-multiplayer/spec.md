# 水方块卡顿修复# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（输入房间码加入联机）
- 游戏内右上角新增「创建房间」按钮，创建后显示房间码供他人加入
# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（输入房间码加入联机）
- 游戏内右上角新增「创建房间」按钮，创建后显示房间码供他人加入
- 新增联机共编：房主与加入者可实时同步世界编辑操作
# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（输入房间码加入联机）
- 游戏内右上角新增「创建房间」按钮，创建后显示房间码供他人加入
- 新增联机共编：房主与加入者可实时同步世界编辑操作
- 房间码格式：`XXXXX-XX`（大写字母 + 数字随机组成）

## Impact
- Affected specs:# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（输入房间码加入联机）
- 游戏内右上角新增「创建房间」按钮，创建后显示房间码供他人加入
- 新增联机共编：房主与加入者可实时同步世界编辑操作
- 房间码格式：`XXXXX-XX`（大写字母 + 数字随机组成）

## Impact
- Affected specs: extend-pixel-game-features（水物理、切换按钮）、create-pixel-sandbox-game（# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（输入房间码加入联机）
- 游戏内右上角新增「创建房间」按钮，创建后显示房间码供他人加入
- 新增联机共编：房主与加入者可实时同步世界编辑操作
- 房间码格式：`XXXXX-XX`（大写字母 + 数字随机组成）

## Impact
- Affected specs: extend-pixel-game-features（水物理、切换按钮）、create-pixel-sandbox-game（基础交互）
- Affected code: `game.js`（物理性能优化、背包栏、联机同步）、`index.html`（登录# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（输入房间码加入联机）
- 游戏内右上角新增「创建房间」按钮，创建后显示房间码供他人加入
- 新增联机共编：房主与加入者可实时同步世界编辑操作
- 房间码格式：`XXXXX-XX`（大写字母 + 数字随机组成）

## Impact
- Affected specs: extend-pixel-game-features（水物理、切换按钮）、create-pixel-sandbox-game（基础交互）
- Affected code: `game.js`（物理性能优化、背包栏、联机同步）、`index.html`（登录界面、背包栏、房间 UI）、`style# 水方块卡顿修复 + 背包栏 + 联机功能 Spec

## Why
放置水后游戏会突然卡住（每帧全量扫描所有格子 + 每帧分配新数组导致 GC 压力与性能崩塌）；同时用户希望将单击切换改为背包栏选择，并新增基于房间码的联机共编功能，让朋友可以一起玩。

## What Changes
- 修复水方块卡顿：只追踪含水的格子，避免每帧全量扫描与重复分配
- **BREAKING**：右下角切换按钮改为点击后弹出背包栏，在背包栏中选择方块类型（替代原单击循环切换）
- 新增登录界面：进入游戏前需创建临时账号（昵称）
- 登录后提供两个入口：开始游戏（单人）、进入房间（输入房间码加入联机）
- 游戏内右上角新增「创建房间」按钮，创建后显示房间码供他人加入
- 新增联机共编：房主与加入者可实时同步世界编辑操作
- 房间码格式：`XXXXX-XX`（大写字母 + 数字随机组成）

## Impact
- Affected specs: extend-pixel-game-features（水物理、切换按钮）、create-pixel-sandbox-game（基础交互）
- Affected code: `game.js`（物理性能优化、背包栏、联机同步）、`index.html`（登录界面、背包栏、房间 UI）、`style.css`（新增 UI 样式）、`README.md`（联机玩法说明）
- 新增依赖：PeerJS（通过 CDN 引入，用于 WebRTC P2P 联机信令）

## ADDED Requirements

###